<h1>Artists</h1>
<table class="table">
	<tr>
		<th>ID</th>
		<th>
			<?php
			// TODO move to controller
			$order = empty($this->query['order']) ? 'name' : $this->query['order'];
//			else if ($this->query['order'] !== 'name') {
//				$order
//			}
//			$order = (!empty($this->query['order']) && $this->query['order'] !== 'name') ? 'name' : '-name'
			?>
			<a href="/artist?page=<?= $this->artistCollection->getCurrentPageNumber(); ?>&order=<?= $order ?>">Name</a>
		</th>
	</tr>
	<?php foreach ($this->artistCollection as $artist) { ?>
		<tr>
			<td><?php echo $this->escapeHtml($artist['id']);?></td>
			<td><?php echo $this->escapeHtml($artist['name']);?></td>
		</tr>
	<?php } ?>
</table>
<?php echo $this->PaginationControl($this->artistCollection, 'partial/paginator/apigility.phtml', 'artist/default'); ?>